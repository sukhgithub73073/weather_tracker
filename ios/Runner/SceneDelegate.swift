import Flutter
import UIKit

/// UIScene lifecycle entry point – required by the iOS 26+ SDK.
///
/// `FlutterSceneDelegate` creates the window with the FlutterViewController
/// from Main.storyboard and forwards scene events (open URL, universal links,
/// foreground/background) to plugins. Override methods here only when the app
/// needs custom scene handling.
class SceneDelegate: FlutterSceneDelegate {
}
